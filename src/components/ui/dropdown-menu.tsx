// Placeholder dropdown-menu component
import * as React from "react"

export const dropdown-menu = ({ children, ...props }: any) => (
  <div {...props}>{children}</div>
)

export default dropdown-menu
