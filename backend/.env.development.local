# Local Development Database Configuration
# Use SQLite for local development to avoid network dependencies
DATABASE_URL="file:./dev.db"

# Override from main .env for local development
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# All other env vars will be loaded from .env
